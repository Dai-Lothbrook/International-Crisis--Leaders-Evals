# P01 Block B — Implementation Report

Block B was completed without target-model or paid judge calls. B1 now freezes the single Evidentiary Responsiveness construct, five coverage families, 10-case/6-Core/4-Stress architecture, exactly F1–F5, relational F2, supporting F4, renamed F5, and two-case Authority robustness (`BL+AUTH`/`SD+AUTH`) without substantive evidence. B2 implements repetition-preserving median CellRisk, all specified deltas/gaps, cross-repetition comparisons, SD×WD all-pairs ordering, R=3 primary cells, exactly two future R=5 sentinel blocks, A/B/C status accounting, S1–S4, judge blinding, immutable local score lineage, and no composite score. B3 fixes the three targets as `gpt-5.6-sol`, `gpt-4.1-2025-04-14`, and `kimi-k3`; `.env` has no stale model fallback, and the future Run Matrix/config remains authoritative. The reference geometry is 408 target generations.

Modified: `src/mastra/scorers/deterministic_p01.ts`, `judge_types.ts`, `langfuse_scores.ts`; `src/mastra/lib/execution_status.ts`, `langfuse.ts`; `src/mastra/renderers/p01_renderer.ts`; `src/mastra/runners/validate_block_a_p01.ts`; `experiment/common/design_decision_log.md`; and `package.json`.

Created: `src/mastra/scorers/judge_input.ts`; `src/mastra/runners/validate_block_b_p01.ts`; and, under `experiment/P01/final/`, `construct_card_v1.0.md`, `failure_taxonomy_v1.0.md`, `scoring_spec_v1.0.md`, `prompt_v1.0.md`, `case_template_v1.0.json`, `run_config_schema_v1.0.json`, `judge_config_v1.0.json`, `calibration_fixtures_v1.0.jsonl`, `langfuse_resources_v1.0.json`, and `P01_FINAL_EXPERIMENT_PROTOCOL_v1.0.md`.

Langfuse now contains Judge A evaluator `cmtia7cvq0fiyad0fm07q1zjp`, Judge B evaluator `cmtia7d380e7zad0jv70szpd0`, and disabled rules `cmtia7daq0fj3ad0f2n6nbw0w` / `cmtia7dhs0fj8ad0fidflmi4s`. Each rule requires generation type/name plus three exact P01 Final metadata filters, preventing Alpha/unrelated matching. Local records retain run, attempt, trace, observation, judge, rubric, and criterion lineage. The Final renderer serializes only `model_visible` and rejects hidden-field leakage.

Typecheck, Block A regression, Block B formulas/statuses/blinding/renderer/config checks, and live Langfuse scope verification all passed. Limitation: Langfuse’s native evaluator surface produces one categorical score per evaluator and its stable model configuration exposes provider/model, not reasoning effort; see the [LLM-as-a-Judge documentation](https://langfuse.com/docs/evaluation/evaluation-methods/llm-as-a-judge) and [Evaluator API reference](https://api.reference.langfuse.com/#tag/evaluators). Therefore the external two-call pipeline remains authoritative for four individual categorical scores per judge and pins HIGH locally. Rules remain disabled pending calibration and integrated smoke. Block C must build/review the 10 concrete cases, select Authority and sentinel identities, and freeze the exact Final Run Matrix.
